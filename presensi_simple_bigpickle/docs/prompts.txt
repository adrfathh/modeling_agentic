# Chain of Truth — Prompt Chain
# Project: Sistem Presensi Kehadiran Siswa (Zero-Trust Attendance Protocol)
# School: SMA Muhammadiyah Kasihan
# Date: 2026-07-09

## Prompt 1 — Phase 1: Core Scaffold

"""
Build a runnable frontend prototype for "Sistem Presensi Kehadiran Siswa Berbasis
Kelas (Zero-Trust Attendance Protocol)" — SMA Muhammadiyah Kasihan.

Dual platform: Mobile App (Admin IT, Guru, Siswa) + Web Portal (Orang Tua, Kepala
Sekolah). Use Next.js 16 (App Router) + TypeScript + Tailwind CSS v4. Routes must
match IA spec exactly (flat paths, no /mobile/ /web/ prefix — use route groups).

Source of Truth: docs/srs.md (SRS-FR-*, SRS-ARCH-*, SRS-NFR-*, business rules,
Data Integrity Clause), docs/information_architecture.md (route map, segmented
tabs, per-role hierarchy).

Out-of-scope: presensi gerbang/pagi terpusat, remote unfreeze, biometric trigger
from student device, forgot password, payment gateway, real backend/database,
direct Dapodik integration.

Create: core lib files (types.ts, api.ts, auth-context.tsx, crypto-utils.ts),
root layout, globals.css, login page, 404 page, reusable components
(SegmentedTabs, DateNavigator), (mobile)/ and (web)/ route groups with layouts,
all 10 mobile pages + 6 web pages, with all types building cleanly.
"""

## Prompt 2 — Phase 2A: IA-Based Routing & Layout

"""
Read docs/srs.md and docs/information_architecture.md. Move all page files to the
new route groups (mobile)/ and (web)/. Update login redirects to flat paths.
Fix all TypeScript errors and ensure the build passes.
"""

## Prompt 3 — Phase 2B: Design System

"""
Read docs/design_system.md (Source of Truth #3). Apply the 6 mandatory functional
color tokens (Active Primary #3182CE, Attend/Approve #48BB78, Waiting/Freeze
#805AD5, Absent/Reject #E53E3E, 1/2 Day/Late #ECC94B, Unavailable/Offline
#A0AEC0), the mandatory UI components (segmented tab nav with active underline,
anti-screenshot barcode with 30s countdown ring, scanner gated by Geofence,
vertical attendance timeline with per-status dots, Donut Chart with bold center
% + "Today's Attendance Percentage" subtitle, Indeks Keterlambatan bar chart &
Alpa distribution chart), and enforce Data Integrity Clause (Waiting/Freeze
#805AD5 and Offline/Unavailable #A0AEC0 never blend into approved/final states).
"""

## Prompt 4 — Phase 2C: User Flows & System Logics

"""
Read docs/user_flows/*.md (UC-001 to UC-006) and docs/system_logics/*.md
(SYS-UC-001 to SYS-UC-006), together with docs/data_model.md (Source of Truth
#4). Implement: JWT/RBAC validation on every request; AES-256-CBC + HMAC-SHA256
signature verification; Geofence ≤100m + terlambatmenit calculation; EAR-based
liveness (EAR > 0.2) + Count_fail tracking + Freeze at ≥3; audit-trailed manual
bypass (GPS + UUID + mandatory reason); Freeze/Unfreeze restricted to Admin IT
physical verification; analytics aggregation strictly excluding "Waiting for
Approval" records.
"""

## Prompt 5 — Phase 3: Finalization

"""
Build and finalize the runnable frontend prototype based on all Source of Truth
documents (srs.md, information_architecture.md, design_system.md, data_model.md,
user_flows/*, system_logics/*). Modern frontend stack: React/Next.js, Tailwind
CSS, localStorage for state persistence (dummy API layer). Must be runnable with
npm install && npm run dev. Demonstrate all 6 core flows end-to-end using dummy
data. Create README.md, IMPLEMENTATION_NOTES.md, and this prompts.txt.
"""
